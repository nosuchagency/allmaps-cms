const state = {
    user: window.user
};

const getters = {
    user: state => state.user
};

const actions = {

};

const mutations = {

};

export default {
    state,
    getters,
    actions,
    mutations
}